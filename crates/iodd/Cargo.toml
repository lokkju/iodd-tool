[package]
name = "iodd"
version = "0.1.0"
description = "Create, convert, and verify contiguous fixed-size VHD/RMD virtual disks for IODD hardware devices"
readme = "../../README.md"
keywords = ["iodd", "vhd", "ntfs", "virtual-disk"]
categories = ["command-line-utilities", "filesystem"]
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ntfs-contig.workspace = true

clap.workspace = true
thiserror.workspace = true
uuid.workspace = true
serde.workspace = true
serde_json.workspace = true
walkdir.workspace = true

[dev-dependencies]
assert_cmd.workspace = true
predicates.workspace = true
tempfile.workspace = true

[features]
# Integration tests needing sudo and a loopback NTFS volume.
ntfs-integration = ["ntfs-contig/ntfs-integration"]

[lints]
workspace = true
